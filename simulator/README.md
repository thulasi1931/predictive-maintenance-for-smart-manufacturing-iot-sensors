# IoT Sensor Simulator

This simulates IoT hardware in software by generating a new sensor reading every five seconds and sending it to the Flask API.

1. Start the backend: `python backend/app.py`
2. In another terminal run: `python simulator/sensor_simulator.py`
3. Stop the simulator with `Ctrl + C`.
