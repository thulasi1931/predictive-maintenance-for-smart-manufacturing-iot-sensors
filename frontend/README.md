# React Dashboard

1. Open a terminal in the `frontend` folder.
2. Run `npm install` once.
3. Run `npm run dev`.
4. Open the local URL shown by Vite (usually `http://localhost:5173`).

Start the Flask backend first from the project root:

```powershell
python backend/app.py
```
